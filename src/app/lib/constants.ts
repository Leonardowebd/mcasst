/* ── Site-wide constants ─────────────────────────────────────────────────
   Centralized so a single edit updates every CTA across the site. */

/** WhatsApp contact link used by every "Entrar em contato" CTA. */
export const WHATSAPP_URL =
  "https://wa.me/5577999160302?text=Vim%20pelo%20Site%20e%20quero%20saber%20mais%20sobre%20a%20Mascatis";

/* ── Discrete gold accent ────────────────────────────────────────────────
   A muted champagne gold reserved for the finest UI details (CTA borders,
   menu indices, active indicators). Kept low-alpha so it only whispers. */

/** Champagne-gold hex used for thin gold details. */
export const GOLD = "#c8a86b";
/** Same gold as an "r, g, b" triplet for inline rgba() composition. */
export const GOLD_RGB = "200, 168, 107";
